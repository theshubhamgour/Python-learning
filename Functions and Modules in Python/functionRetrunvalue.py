
# return is going to add two values and store them in variable c
# as we want our function to return some value therefore we will write return c since the addition of the variable is sotred in c
def add(a,b):
    c=a+b
    return c
# now we will pass arguments and print them but for printing them we will make a variable where the result is going to be stored
# as return c is storing the values(result) we need something to catch the value here we will use result for doing the same
result= add(100, 200)
print(result)    

